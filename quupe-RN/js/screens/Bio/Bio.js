import React from 'react';
import { ScrollView } from 'react-native';
import BioForm from '../../components/UpdateProfileForm';

const Bio = () => (
    <ScrollView>
        <BioForm />
    </ScrollView>
);

export default Bio;
