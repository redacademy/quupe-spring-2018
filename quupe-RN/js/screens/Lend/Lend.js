import React from 'react';
import { ScrollView } from 'react-native';
import LendForm from '../../components/LendForm';

const Lend = () => (
    <ScrollView>
        <LendForm />
    </ScrollView>
);

export default Lend;
